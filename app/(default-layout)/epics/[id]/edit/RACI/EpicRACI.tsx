// EpicRACI.tsx was left behind during refactor.
// RACI editing is now handled in RaciMatrixSection; keep this file as a harmless placeholder
// so any imports don't break until we fully remove edit-time RACI surfaces.

export default function EpicRACI() {
  return null;
}
